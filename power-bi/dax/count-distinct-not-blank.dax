Distinct Records Without Blank =
COUNTROWS (
    FILTER (
        VALUES ( 'FactTable'[BusinessKey] ),
        NOT ISBLANK ( 'FactTable'[BusinessKey] )
            && 'FactTable'[BusinessKey] <> ""
    )
)
